{{- if .Values.chatbot.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: crapi-chatbot-secret
  namespace: {{ .Values.mailhog.namespace }}
  labels:
    release: {{ .Release.Name }}
type: Opaque
stringData:
  openai-api-key: {{ .Values.chatbot.openaiApiKey | quote }}
{{- end }}
